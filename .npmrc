# zeego@3.0.6 peer-pins react-native-ios-context-menu@3.1.0 + react-native-ios-utilities@5.1.2,
# but Phase 1 (01-02) installs the newer 3.2.1 / 5.2.0 (verified to compile on RN 0.81 / New Arch
# with the RCT-Folly patch in patches/). legacy-peer-deps lets npm tolerate zeego's stale exact-peer.
# DEFERRED: when context menus are actually built (later phase), re-evaluate aligning these versions
# with zeego's expected peers (or bump zeego) and remove this if no longer needed.
legacy-peer-deps=true
